# Reference: http://i3wm.org/docs/userguide.html

# Windows key is $mod
set $mod Mod4

# Gaps, requires i3-gaps
gaps inner 6
smart_gaps on

# Default layout is tabbed
workspace_layout tabbed

# Font for window titles. Use xfontsel to find fontstrings, or just use pango:
#font pango:Terminus 14px
#font pango:Anonymous Pro for Powerline 14px
#font pango:Monofur, FontAwesome, Noto Color Emoji, 12
#font pango:Roboto Mono, FontAwesome, Noto Color Emoji, 11
#font pango:Source Code Pro for Powerline, FontAwesome, 14
font pango:B612 Mono for Powerline, FontAwesome, 13

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec terminator

# kill focused window
bindsym $mod+Shift+Q kill

bindsym $mod+d exec rofi -show combi -display-combi '> ' -display-drun '$' -sidebar-mode
bindsym $mod+p exec PASSWORD_STORE_DIR="/home/rixx/.local/share/password-store" rofi-pass

# start applications
bindsym $mod+Shift+F exec firefox-nightly
bindsym $mod+Shift+C exec electron ~/.config/dotfiles/calendar
bindsym $mod+Shift+S exec signal-desktop --start-in-tray
bindsym $mod+l exec 	i3lock \
	-t -i $(find ~/lib/pics/wallpapers/processed | shuf -n1) \
	--timepos='x+110:h-70' \
	--datepos='x+43:h-45' \
	--clock --date-align 1 --datestr "Und Sie sind?" \
	--insidecolor=00000000 --ringcolor=ffffffff --line-uses-inside \
	--keyhlcolor=d23c3dff --bshlcolor=d23c3dff --separatorcolor=00000000 \
	--insidevercolor=00000000 --insidewrongcolor=d23c3dff \
	--ringvercolor=ffffffff --ringwrongcolor=ffffffff --indpos='x+280:h-70' \
	--radius=20 --ring-width=4 --veriftext='' --wrongtext='Nochmaaal!' \
	--verifcolor=ffffffff --timecolor=ffffffff --datecolor=ffffffff \
	--noinputtext='' --force-clock
bindsym $mod+m exec "rofimoji"

# focus window with cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window with cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# go to container with message
bindsym $mod+x [urgent=latest] focus

# change container layout (stacked, tabbed, default)
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# function keys
bindsym XF86AudioMute exec /usr/bin/amixer -D pulse -q sset Master toggle  # <F1>
bindsym XF86AudioLowerVolume exec /usr/bin/amixer -D pulse -q sset Master 5%-  # <F2>
bindsym XF86AudioRaiseVolume exec /usr/bin/amixer -D pulse -q sset Master 5%+  # <F3>
bindsym XF86AudioMicMute exec /usr/bin/amixer -D pulse -q sset Capture toggle  # <F4>
bindsym XF86MonBrightnessDown exec /usr/bin/xbacklight -dec 5  # <F5>
bindsym XF86MonBrightnessUp exec /usr/bin/xbacklight -inc 10  # <F6>
bindsym XF86Display exec /usr/bin/wxcam  # <F7>
bindsym XF86Tools exec /usr/bin/pcmanfm  # <F9>
#bindsym XF86Search
#bindsym XF86WakeUp exec notify-send 'hi'  # sent on long fn press
# bindsym Multi_key exec /usr/bin/scrot  # 'Druck'/'Print'

bindsym $mod+s exec deepin-screenshot -s /home/rixx/tmp/screenshots

# switch to workspace;
bindsym $mod+1 workspace 1: term 
bindsym $mod+2 workspace 2: web  
bindsym $mod+3 workspace 3: dev  
bindsym $mod+4 workspace 4: mail 
bindsym $mod+5 workspace 5: im  
bindsym $mod+6 workspace 6: rand  

# default output location with the docking station
workspace "1: term " output eDP1
workspace "2: web  " output DP2-1
workspace "3: dev  " output eDP1
workspace "4: mail " output DP2-1
workspace "5: im  " output DP2-1

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1: term 
bindsym $mod+Shift+2 move container to workspace 2: web  
bindsym $mod+Shift+3 move container to workspace 3: dev  
bindsym $mod+Shift+4 move container to workspace 4: mail 
bindsym $mod+Shift+5 move container to workspace 5: im  
bindsym $mod+Shift+6 move container to workspace 6: rand  

# reload the configuration file
bindsym $mod+Shift+D reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart

# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym h resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
   status_command /usr/bin/py3status -b -c ~/.config/i3/status.conf
   #font pango:Roboto Mono, FontAwesome, 11
   #font pango:Source Code Pro for Powerline, FontAwesome, 14
   font pango:B612 Mono for Powerline, FontAwesome, 13

   position top
   workspace_buttons yes
   strip_workspace_numbers yes
   tray_output eDP1

   colors {
      background #212121
      statusline #BDBDBD
      separator #9E9E9E
      focused_workspace #212121 #BDBDBD #212121
      urgent_workspace #D84315 #D84315 #212121
      inactive_workspace #212121 #212121 #BDBDBD
   }
}

#Colors
client.focused    #212121 #BDBDBD #212121 #212121 #212121
client.urgent     #212121 #D84315 #212121 #212121 #212121
client.unfocused  #212121 #212121 #BDBDBD #212121 #212121
client.background #212121


# Border style
new_window pixel 1
new_float pixel 0
hide_edge_borders smart
focus_follows_mouse no

#move to correct workspace
assign [class="Firefox"] 2: web  
assign [class="Nightly"] 2: web  
assign [window_role="neomutt"] 4: mail 
assign [class="Slack"] 5: im  
assign [class="whats-app-nativefier-d52542"] 5: im  
assign [class="Signal"] 5: im  
assign [class="spotify"] 6: rand  

# Floating applications
for_window [class="Arandr"] floating enable sticky enable border normal
for_window [class="feh"] floating enable sticky enable border normal
for_window [class="Lxappearance"] floating enable sticky enable border normal
for_window [class="Pavucontrol"] floating enable sticky enable border normal
for_window [class="Pinentry"] floating enable sticky enable border normal
for_window [class="^Steam$" title="^Friends List$"] floating enable sticky enable border normal
for_window [class="^Steam$" title="Steam - News"] floating enable sticky enable border normal
for_window [class="^Steam$" title=".* - Chat"] floating enable sticky enable border normal
for_window [class="^Steam$" title="^Settings$"] floating enable sticky enable border normal
for_window [class="^Steam$" title=".* - event started"] floating enable sticky enable border normal
for_window [class="^Steam$" title="^Steam - Self Updater$"] floating enable sticky enable border normal

# Just hope to catch a few more
for_window [window_role="About"]                       floating enable sticky enable border normal
for_window [window_role="bubble"]                       floating enable sticky enable border normal
for_window [window_role="pop-up"]                       floating enable sticky enable border normal
for_window [window_role="task_dialog"]                  floating enable sticky enable border normal
for_window [window_role="Preferences"]                  floating enable sticky enable border normal
for_window [window_type="dialog"]                       floating enable sticky enable border normal
for_window [window_type="menu"]                         floating enable sticky enable border normal
for_window [class="(?i)mplayer"]                        floating enable sticky enable border normal


# Create Log out, Reboot, Poweroff bindings
mode "(L)ock screen, (E)xit X, (S)uspend, (R)eboot, (P)oweroff" {
        bindsym r exec reboot
        bindsym e exit
        bindsym p exec poweroff
        bindsym s exec systemctl suspend

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+Shift+e mode "(L)ock screen, (E)xit X, (S)uspend, (R)eboot, (P)oweroff"

# $mod+current_workspace switches back to last workspace
workspace_auto_back_and_forth yes

# exec_always "pgrep -x neomutt || (terminator -r neomutt -T neomutt -x neomutt &)"
